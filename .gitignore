node_modules/
dist/

# Runtime data — ditulis ulang oleh app tiap siklus monitoring.
# JANGAN diversion agar `git pull` tidak selalu bentrok.
data/daily_stats.json
data/history.json
data/network_stats.json
data/status_snapshot.json

# Logs (harian, selalu berubah)
logs/

# v2 Stechoq Pulse — akun & rahasia sesi (JANGAN di-commit)
v2/users.json
v2/.pulse-secret

# v1 dashboard — akun & rahasia sesi (JANGAN di-commit)
data/users.json
data/.dashboard-secret

# Catatan: data/devices.json TETAP dilacak (daftar device / konfigurasi).
