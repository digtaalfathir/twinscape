node_modules/
dist/

# Arsip lama — disimpan lokal saja, JANGAN di-push
unused/

# Runtime data v1 (classic) — ditulis ulang oleh app tiap siklus monitoring.
# JANGAN diversion agar `git pull` tidak selalu bentrok.
legacy/data/daily_stats.json
legacy/data/history.json
legacy/data/network_stats.json
legacy/data/status_snapshot.json

# Logs (harian, selalu berubah)
legacy/logs/
twinscape/logs/

# Twinscape (v2) — akun & rahasia sesi (JANGAN di-commit)
twinscape/users.json
twinscape/.pulse-secret

# v1 classic — akun & rahasia sesi (JANGAN di-commit)
legacy/data/users.json
legacy/data/.dashboard-secret

# Catatan: legacy/data/devices.json TETAP dilacak (daftar device / konfigurasi).
